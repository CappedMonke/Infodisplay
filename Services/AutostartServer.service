[Unit]
Description=Autostart Server Service
After=network.target

[Service]
ExecStart=/bin/bash -c 'source %INSTALL_DIR%/.venv/bin/activate && exec python3 %INSTALL_DIR%/Server/FlaskServer.py'
Restart=always
User=%u

[Install]
WantedBy=default.target
